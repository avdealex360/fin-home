# Доступ по IP без домена.
# HTTP/3 (QUIC) отключён — иначе браузер может давать ERR_SSL_PROTOCOL_ERROR,
# если UDP/443 недоступен снаружи.
{
	servers {
		protocols h1 h2
	}
}

:443 {
	tls internal
	reverse_proxy budget-app:8000
}

:80 {
	redir https://{host}{uri} permanent
}
